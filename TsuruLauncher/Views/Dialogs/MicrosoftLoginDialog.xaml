<Window x:Class="TsuruLauncher.Views.Dialogs.MicrosoftLoginDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
        mc:Ignorable="d"
        Title="Microsoft Login" Height="600" Width="500"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent">

    <Border Background="{DynamicResource Surface3Brush}" CornerRadius="12" BorderBrush="{DynamicResource GlassBorderBrush}" BorderThickness="1">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="40"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Title Bar -->
            <Grid Grid.Row="0" Background="{DynamicResource Surface4Brush}" MouseLeftButtonDown="TitleBar_MouseDown">
                <Border CornerRadius="12,12,0,0" Background="Transparent"/>
                <TextBlock Text="Sign in with Microsoft" 
                           Foreground="{DynamicResource TextPrimaryBrush}" 
                           VerticalAlignment="Center" 
                           Margin="15,0,0,0"
                           FontWeight="SemiBold"/>
                
                <Button Content="✕" 
                        HorizontalAlignment="Right" 
                        Width="40" 
                        Background="Transparent" 
                        BorderThickness="0" 
                        Foreground="{DynamicResource TextPrimaryBrush}"
                        Click="CloseButton_Click"
                        Cursor="Hand"/>
            </Grid>

            <!-- WebView -->
            <Border Grid.Row="1" Background="{DynamicResource Surface1Brush}" Margin="2,0,2,2" CornerRadius="0,0,10,10" ClipToBounds="True">
                <wv2:WebView2 x:Name="LoginWebView" Source="about:blank"/>
            </Border>
            
            <!-- Loading Overlay -->
            <Grid Grid.Row="1" x:Name="LoadingOverlay" Background="{DynamicResource ScrimBrush}" Visibility="Collapsed">
                <ProgressBar IsIndeterminate="True" Width="200" Height="4" VerticalAlignment="Center"/>
                <TextBlock Text="Processing login..." Foreground="{DynamicResource TextPrimaryBrush}" HorizontalAlignment="Center" Margin="0,30,0,0" VerticalAlignment="Center"/>
            </Grid>
        </Grid>
    </Border>
</Window>