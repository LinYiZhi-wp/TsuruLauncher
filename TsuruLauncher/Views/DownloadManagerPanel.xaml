<UserControl x:Class="TsuruLauncher.Views.DownloadManagerPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="350">
    <UserControl.Resources>
        <Storyboard x:Key="PulsateAnimation" RepeatBehavior="Forever" AutoReverse="True">
            <DoubleAnimation Storyboard.TargetName="FabButton" 
                              Storyboard.TargetProperty="(UIElement.Effect).(DropShadowEffect.BlurRadius)"
                              From="15" To="35" Duration="0:0:1"/>
            <DoubleAnimation Storyboard.TargetName="FabButton" 
                              Storyboard.TargetProperty="Opacity"
                              From="1.0" To="0.8" Duration="0:0:1"/>
        </Storyboard>
    </UserControl.Resources>

    <Grid>
        <!-- Logic removed: Floating button is now handled by Main Window TitleBar button -->
    </Grid>
</UserControl>